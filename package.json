{
  "name": "provider-example",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "BUILD_EXPORT=true next build && touch out/.nojekyll",
    "predeploy": "npm run build",
    "deploy": "gh-pages -t -d out",
    "start": "next start"
  },
  "dependencies": {
    "@bywise/provider": "^1.1.1",
    "@bywise/web3": "^1.0.3",
    "next": "14.2.3",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
